<%= form_with(url:  hostel_owner_path(hostel_owner),model: hostel_owner) do |form| %>
  <% if hostel_owner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hostel_owner.errors.count, "error") %> prohibited this hostel_owner from being saved:</h2>

      <ul>
        <% hostel_owner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :phone %><br>
    <%= form.text_field :phone %>
  </div>

  <div>
    <%= form.label :email %><br>
    <%= form.text_field :email%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
