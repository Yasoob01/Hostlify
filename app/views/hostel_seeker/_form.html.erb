<%= form_with(url:  hostel_seeker_path(hostel_seeker), model: hostel_seeker) do |form| %>
  <% if hostel_seeker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hostel_seeker.errors.count, "error") %> prohibited this hostel_seeker from being saved:</h2>

      <ul>
        <% hostel_seeker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :phone %><br>
    <%= form.text_field :phone %>
  </div>

  <div>
    <%= form.label :email %><br>
    <%= form.text_field :email%>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
